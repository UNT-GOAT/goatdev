name: Deploy Frontend

on:
  push:
    branches: [main]
    paths:
      - "goat-app/**"
      - ".github/workflows/deploy-frontend.yml"
  workflow_dispatch:

jobs:
  deploy-setup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Generate config
        run: |
          cat > goat-app/setup/config.js << EOF
          const CONFIG = {
            PASS_HASH: "${{ secrets.PASS_HASH }}",
            CAPTURE_API: "http://${{ secrets.TAILSCALE_IP }}:5000",
            HEATER_API: "http://${{ secrets.TAILSCALE_IP }}:5002",
            FOCUS_API: "http://${{ secrets.TAILSCALE_IP }}:8080",
            S3_BUCKET: "${{ secrets.S3_TRAINING_BUCKET }}",
            S3_REGION: "us-east-2",
          };
          EOF

      - name: Check for setup page changes
        id: check
        run: |
          if git diff --name-only HEAD~1 | grep -q "goat-app/setup/"; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Deploy training UI to S3
        if: steps.check.outputs.changed == 'true' || github.event_name == 'workflow_dispatch'
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: us-east-2
        run: |
          aws s3 sync goat-app/setup/ s3://goat-web-937249941844/setup/
          echo "Setup UI deployed"

      - name: Invalidate CloudFront
        if: steps.check.outputs.changed == 'true' || github.event_name == 'workflow_dispatch'
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: us-east-2
        run: |
          aws cloudfront create-invalidation --distribution-id EB8KCE4E7TGI4 --paths "/setup/*"

  deploy-prod:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Check for prod changes
        id: check
        run: |
          if git diff --name-only HEAD~1 | grep -v "goat-app/training/" | grep -q "goat-app/"; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Build and deploy prod
        if: steps.check.outputs.changed == 'true'
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: us-east-2
        run: |
          echo "Prod build skipped - React not configured yet"

      - name: Invalidate CloudFront
        if: steps.check.outputs.changed == 'true'
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: us-east-2
        run: |
          aws cloudfront create-invalidation --distribution-id EB8KCE4E7TGI4 --paths "/*"
